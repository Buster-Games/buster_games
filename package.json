{
  "name": "buster-games",
  "version": "0.1.0",
  "private": true,
  "description": "Buster Games — a mobile web retro game app for Lara",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-assets.js",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:assets": "node scripts/check-assets.js"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.0"
  }
}
